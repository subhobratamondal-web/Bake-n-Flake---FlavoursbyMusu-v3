services:
  - type: web
    name: bake-n-flake-web
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: SPREADSHEET_ID
        sync: false # You will provide this in Render Dashboard
